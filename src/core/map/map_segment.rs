/// map_segment.rs
/// A continuous map comprised of one (such as a single level in a dungeon) 
/// or several (such as connected outdoors areas) different areas 
/// 

struct MapSegment {
    // static LevelMapList cache;

    // Act* act;
    // const Level* level;
    // int width, height;
    // int posX, posY;
    // Matrix<CollisionFlag> mapPoints;
    // RoomList addedRooms;

    // Level* cachedLevel;
    // CRITICAL_SECTION* lock;
}

impl Map for MapSegment {

}